Options -Indexes

<FilesMatch "(^\.|^(README|DEPLOYMENT)\.md$|^package(-lock)?\.json$|^vite\.config\.js$|^eslint\.config\.js$|^init_db\.php$)">
    Require all denied
</FilesMatch>

<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
</IfModule>
